<div class="list-container">
  <div class="list-header">
    <FaIcon @icon={{@model.iconName}} @size="2x" />
    <h2>{{@model.displayName}}</h2>
  </div>
  <Common::Separator />
  <List::Entries @entries={{@model.entries}} />
</div>
<List::InputFooter @list={{@model}} />